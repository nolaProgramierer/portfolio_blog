<header class ="navbar navbar-static-top">
  <div class="container">
    <nav>
      <% if logged_in? %>
      <p><%= link_to "Articles Listing", articles_path, class: "navbar-text navbar-right" %></p>
      <% end %>
      <ul class="nav navbar-nav navbar-left">
        <li id="article-listing-link"><%= link_to '&#10550; Home'.html_safe, root_path(anchor: "article-block"), id: "back-button" %></li>
        <% if logged_in? %>
      <li class="dropdown">
        <a href="#" class="dropdown-toggle" data-toggle="dropdown">
          Admin <b class="caret"></b>
        </a>
        <ul class="dropdown-menu">
          <li><%= link_to "Users", users_path %></li>
          <li><%= link_to "Current User", current_user %></li>
          <li class="divider"></li>
          <li><%= link_to "Log out", logout_path, method: :delete %></li>
        </ul>
      </li>
      <% end %>
    </ul>
    </nav>
  </div>
</header>
